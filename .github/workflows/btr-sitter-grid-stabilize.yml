name: BTR Sitter Grid Stabilize
on:
  push:
    branches:
      - btr-sitter-grid-stabilize-20260815
    paths:
      - .github/workflows/btr-sitter-grid-stabilize.yml
permissions:
  contents: write
jobs:
  apply-test-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: btr-sitter-grid-stabilize-20260815
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply sitter grid stabilization
        run: python .github/stabilize-sitter-grid.py
      - name: Syntax and regression
        run: |
          node --check sitter-mode.js
          node --check shared-care.js
          node --check app.js
          npm test
      - name: Verify scope
        run: |
          git diff --check
          changed="$(git diff --name-only | sort)"
          expected="$(printf '%s\n' index.html styles.css sw.js tests/regression.mjs | sort)"
          printf 'Changed files:\n%s\n' "$changed"
          test "$changed" = "$expected"
      - name: Commit isolated stabilization
        run: |
          git config user.name "BTR Sitter Grid Stabilize"
          git config user.email "actions@users.noreply.github.com"
          git add index.html styles.css sw.js tests/regression.mjs
          git commit -m "Stabilize sitter editor field grid"
          git push origin HEAD:btr-sitter-grid-stabilize-20260815
